<phpunit bootstrap="./TestHelper.php">
    <testsuite name="XenPlus">
        <directory>./</directory>
    </testsuite>

    <!-- Enable this for proper unit testing code coverage reports
    <filter>
        <whitelist>
            <directory suffix=".php">../library/XenPlus</directory>
        </whitelist>
    </filter>
    -->
</phpunit>
